# Backend

FastAPI service for timetable, availability, and file-import workflows.

Run locally from this directory after dependencies are installed:

```bash
python -m uvicorn app.main:app --reload
```
